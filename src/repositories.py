# Đây là nơi viết CRUD tương tác DB
def save_to_db(instance):
    # db.session.add(instance)
    # db.session.commit()
    pass

def get_from_db(model, filters=None):
    # return model.query.filter_by(**filters).all()
    return []
